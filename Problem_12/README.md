## Count Number in Matrix

This program prints a 3×3 matrix and asks the user to enter a number.
It counts how many times the given number appears in the matrix.
The program then displays the total count of that number.
Useful for practicing arrays, nested loops, and user input in C++.

#### Problem -> 12
